# 🎯 QuestGuideRF - Интерактивный Экскурсовод + Веб-сайт
Полноценная система для проведения интерактивных городских квестов:

- **Telegram бот** (Python 3.8+) - ведет пользователя по маршруту, проверяет фото, выдает факты
- **Веб-сайт** (PHP 7.4+) - личный кабинет со статистикой, фотографиями, достижениями
- **Единая БД MySQL** - одна база для бота и сайта
- **Единая папка uploads/** - общее хранилище фото

## ✨ Возможности

### Telegram Бот
- 🗺️ Квест-экскурсии по городам
- 📸 Проверка фото (машинное зрение: CLIP для локации, MoveNet + YOLO ONNX для поз и людей)
- 💰 Оплата через Telegram Payments (ЮKassa) и Telegram Stars; маршруты покупаются за токены
- 🏦 **Банк токенов** — команда `/token`: пополнение (ЮKassa, Telegram Stars), перевод токенов между пользователями, покупка экскурсий за токены, история операций
- 🎫 **Система промокодов** - процентные скидки, фиксированные скидки, бесплатные маршруты с привязкой к конкретным маршрутам
- 🏆 Система достижений (10 готовых)
- 🔐 Админ-панель в боте (управление городами, маршрутами, точками, пользователями, промокодами)
- 🛡️ Антифрод защита
- 🌐 **Команда `/web`** - вход на сайт через Telegram Web App (открывается прямо в Telegram) или в браузере
- 🔍 **Гибкая модерация фото** - автоматические проверки ИЛИ ручная модерация (настраивается в админ-меню)
- 💡 **Система подсказок** - 3 уровня подсказок (легкая, средняя, детальная) с мини-картами для навигации, доступна прямо в меню прохождения квеста
- 🏷️ **Система тегов и фильтрации** - поиск маршрутов по темам, возрасту, сложности, длительности, сезону (30 предустановленных тегов)
- 🎧 **Аудиогид** - голосовое сопровождение точек через Edge TTS (живой нейросетевой голос), русский и английский языки
- 🚫 **Система блокировок** - быстрая блокировка пользователей (1 час, 1 день, 1 месяц, 1 год, навсегда) прямо из бота с указанием причины
- 🌍 **Мультиязычность** - поддержка русского и английского языков, выбор при первом запуске, все тексты локализованы
- ⭐ **Система отзывов** - пользователи могут оценивать маршруты (1-5 звезд) и оставлять текстовые отзывы, команда `/review`
- 📈 **Команда /top** - топ-10 маршрутов по количеству прохождений с средним временем
- 🧩 **Текстовые задания** - загадки и вопросы вместо фото, проверка с учетом опечаток (fuzzy matching)
- 🖼️ **Улучшение фото** - автоматическая обработка загруженных фото (яркость, контраст, резкость, насыщенность)
- 📷 **Фото в подсказках** - возможность прикреплять визуальные подсказки (фото) к текстовым подсказкам для лучшей навигации
- 🎓 **Генерация сертификатов** - автоматическое создание PDF сертификатов о прохождении маршрутов на русском и английском языках
- 📱 **Гибкая проверка подписки** - опциональная проверка подписки на канал, включается/выключается в админ-меню, не блокирует просмотр статистики и настроек
- ⚙️ **Расширенные настройки** - меню настроек с выбором языка, аудио и управлением системой (проверка подписки, режим модерации, уведомления)
- 💳 **Команда /promo** - ввод промокода для получения скидки на маршрут

### Веб-сайт
- 🔐 **Авторизация через Telegram** - Telegram Web App (автоматическая авторизация через initData) или через токены от `/web`
- 📱 **Telegram Web App** - личный кабинет открывается прямо в Telegram без установки приложений, автоматическая авторизация, поддержка тем Telegram
- 📊 Dashboard с статистикой
- 🗺️ История экскурсий
- 📸 Галерея фотографий
- 🏦 Банк — баланс токенов, история пополнений и покупок маршрутов
- 🏆 Коллекция достижений
- 🌓 Светлая/тёмная тема (мгновенное переключение)
- 📱 Mobile-first дизайн
- 📷 Загрузка собственного аватара
- 🤖 SEO: robots.txt, sitemap.xml
- 📄 Кастомные страницы ошибок (404, 403, 500)

### 🌐 Веб-админ-панель
- 📊 **Дашборд с аналитикой** - графики регистраций, прохождений, выручки (Chart.js)
- 🗺️ **Управление маршрутами** - CRUD операции, массовые действия, экспорт CSV/JSON
- 📍 **Управление точками** - создание, редактирование, управление подсказками и аудио
- 💡 **Управление подсказками** - 3 уровня подсказок, загрузка мини-карт и фото-подсказок
- 🏷️ **Управление тегами** - 30 предустановленных тегов, создание новых, назначение маршрутам
- 🎧 **Управление аудио** - генерация через TTS, загрузка записанных аудиофайлов
- 📸 **Модерация фото** - принятие/отклонение с причинами, массовая модерация
- 👥 **Управление пользователями** - список, статистика, блокировка/разблокировка
- 🎫 **Управление промокодами** - создание, редактирование, удаление промокодов с разными типами скидок
- 🏙️ **Управление городами** - создание, редактирование, удаление городов, статистика по городам
- 🏦 **Банк токенов** - балансы пользователей, история транзакций, ручное начисление токенов
- 📈 **Детальная аналитика** - отчеты, метрики, экспорт данных

## 📁 Структура на хостинге REG.RU

```
/var/www/html/                      # Корень сайта (public_html)
│
├── index.php                       # Сайт (PHP 7.4+)
├── dashboard.php
├── routes.php
├── photos.php
├── bank.php                        # Банк токенов (баланс, история покупок)
├── achievements.php
├── settings.php
├── 404.php, 403.php, 500.php      # Страницы ошибок
├── robots.txt                      # SEO
├── sitemap.xml                     # SEO
│
├── api/                            # API сайта
│   ├── upload_avatar.php          # Загрузка аватара
│   ├── logout.php
│   └── ...
│
├── auth/
│   └── telegram.php                # Авторизация через токены бота
│
├── includes/                       # PHP модули
├── assets/                         # CSS/JS
├── pages/
│
├── admin/                          # Веб-админ-панель
│   ├── dashboard.php              # Дашборд с аналитикой
│   ├── routes/                     # Управление маршрутами
│   ├── points/                     # Управление точками
│   ├── hints/                      # Управление подсказками
│   ├── tags/                       # Управление тегами
│   ├── cities/                     # Управление городами
│   ├── bank/                       # Банк токенов (балансы, транзакции, начисление)
│   ├── moderation/                 # Модерация фото
│   ├── users/                      # Управление пользователями
│   ├── analytics/                  # Аналитика
│   └── api/                        # API endpoints
│
├── bot/                            # Telegram бот (Python 3.8+)
│   ├── bot/                        # Основной код
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── loader.py
│   │   ├── models/
│   │   ├── services/
│   │   │   ├── vision.py           # Сервис проверки (CLIP + AKAZE/ORB fallback)
│   │   │   ├── photo_verifier.py   # CLIP — проверка локации по описанию
│   │   │   ├── pose_movenet.py     # MoveNet + YOLOv8 ONNX — позы и детекция людей
│   │   │   ├── admin_notifier.py   # Уведомления админам
│   │   │   └── ...
│   │   ├── routers/
│   │   │   ├── bank.py             # Банк токенов (/token)
│   │   │   ├── admin.py            # Обработка принятия/отклонения фото
│   │   │   └── ...
│   │   └── ...
│   ├── .env                        # Конфиг бота
│   ├── requirements.txt
│   └── start_bot.sh, stop_bot.sh, restart_bot.sh
│
├── photos/                         # Фото пользователей (создаётся ботом)
│   └── {telegram_id}/
│
├── uploads/                        # ОБЩАЯ папка для загрузок
│   ├── avatars/                    # Аватары пользователей
│   │   └── {user_id}/
│   ├── users/                      # Фото пользователей (legacy)
│   ├── reference/                  # Эталонные фото
│   ├── hints/                      # Мини-карты и фото для подсказок
│   ├── audio/points/               # Аудиофайлы для точек
│   └── temp/                       # Временные файлы
├── assets/
│   └── certificate_foto/           # Шаблоны для сертификатов
│       ├── certificate_template.png
│       └── generated/              # Сгенерированные сертификаты
│
├── database/
│   └── schema.sql                  # ОБЩАЯ схема MySQL
│
├── .env                            # Конфиг сайта
└── .htaccess                       # Apache настройки
```

## 🚀 Установка на REG.RU

### Шаг 1: Создание БД

**Через панель REG.RU:**
1. Базы данных → MySQL → Создать
2. Имя: `u3372144_schema`, кодировка: `utf8mb4_unicode_ci`
3. Создать пользователя с полными правами
4. Запомнить: хост, имя БД, пользователь, пароль

### Шаг 2: Загрузка файлов

**Через FTP (FileZilla/WinSCP):**
1. Подключиться к ftp.ваш-домен.ru
2. Перейти в `www`
3. Загрузить:
   - Все файлы из `Site/` → в корень
   - Папку `bot/` → как есть

### Шаг 3: Установка зависимостей бота

**Автоматическая установка (рекомендуется):**
```bash
cd ~/www/questguiderf.ru/bot
chmod +x install.sh
./install.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение (если его нет)
- Установит все зависимости из requirements.txt
- Проверит установку основных модулей

**Если возникли проблемы с установкой torch или ошибка CUDA:**
```bash
cd ~/www/questguiderf.ru/bot
chmod +x reinstall_torch_cpu.sh
./reinstall_torch_cpu.sh
```

Или вручную:
```bash
cd ~/www/questguiderf.ru/bot
source venv/bin/activate
pip uninstall -y torch torchvision torchaudio
pip install torch==2.0.1+cpu --index-url https://download.pytorch.org/whl/cpu
```

### Шаг 4: Настройка

**1. Обновите .env для сайта** (в корне `www/Ваш домен/.env`):
```env
DB_HOST=localhost
DB_NAME=quest_bot
DB_USER=quest_bot_user
DB_PASS=ваш_пароль_mysql
BOT_TOKEN=ваш_токен_от_BotFather
BOT_USERNAME=ваш_бот_username
SITE_URL=https://ваш-домен.ru
DEBUG=false
```

**2. Обновите .env для бота** (`www/Ваш домен/bot/.env`):
```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=ваш_telegram_id,второй_админ_id
DB_HOST=localhost
DB_PORT=3306
DB_USER=quest_bot_user
DB_PASSWORD=ваш_пароль_mysql
DB_NAME=quest_bot
BASE_DIR=/var/www/html
SITE_URL=https://ваш-домен.ru
API_SECRET=случайная_строка_123
```

### Шаг 5: Запуск бота

**Автоматическая установка (рекомендуется):**
```bash
cd ~/www/questguiderf.ru/bot
chmod +x install.sh
./install.sh
```

**Ручная установка:**
```bash
cd ~/www/questguiderf.ru/bot
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
export OPENBLAS_NUM_THREADS=1
export OMP_NUM_THREADS=1
pip install -r requirements.txt
```

**Запуск бота:**
```bash
cd ~/www/questguiderf.ru/bot
chmod +x start.sh
./start.sh
```

**Или вручную:**
```bash
cd ~/www/questguiderf.ru/bot/bot
source ../venv/bin/activate
export OPENBLAS_NUM_THREADS=1
export OMP_NUM_THREADS=1
export PYTHONPATH="$(pwd)/..:${PYTHONPATH}"
python -m bot.main
```

**Полезные команды для screen:**
```bash
screen -ls                    # Список сессий
screen -r questbot            # Подключиться к сессии
# Внутри screen: Ctrl+A, затем D - отключиться
```

### Шаг 6: Проверка работы

1. ✅ Открыть https://ваш-домен.ru
2. ✅ Увидеть кнопку "Login with Telegram"
3. ✅ Авторизоваться
4. ✅ В боте отправить `/start`
5. ✅ В боте отправить `/web` → получить ссылку на сайт

## 🛠 Технологии

**Бот:**
- Python 3.8+
- aiogram 3.4
- MySQL 8.0 (aiomysql)
- OpenCV + MoveNet Lightning ONNX (вместо MediaPipe)
- ONNX Runtime для проверки поз
- Edge TTS для генерации аудио (живой нейросетевой голос)
- SQLAlchemy 2.0 для работы с БД

**Сайт:**
- PHP 7.4+
- MySQL 8.0 (PDO)
- Vanilla JS
- CSS3 (Mobile-first)
- Bootstrap 5.3.0 для админ-панели
- Chart.js 4.4.0 для графиков аналитики
- GD Library для генерации изображений (сертификаты)
- TCPDF для генерации PDF сертификатов

## 📋 База данных (ОБЩАЯ)

**Основные таблицы:**
- `users` - пользователи (с полями блокировки: ban_until, ban_reason, banned_by, banned_at)
- `cities` - города
- `routes` - маршруты (с полями: difficulty, duration_minutes, age_min, age_max, season, max_hints_per_route)
- `points` - точки маршрутов (с полями: audio_enabled, audio_file_path, audio_language)
- `reference_images` - эталонные фото
- `user_progress` - прогресс (бот пишет → сайт читает)
- `payments` - платежи (бот пишет → сайт читает)
- `user_photos` - фотографии (бот пишет → сайт читает)
- `user_sessions` - токены входа (бот создает → сайт использует)
- `achievements` + `user_achievements` - достижения

**Новые таблицы:**
- `hints` - подсказки для точек (3 уровня: легкая, средняя, детальная) с полем `image_path` для фото-подсказок
- `user_hints` - история использования подсказок пользователями
- `tags` - теги для маршрутов (темы, возраст, сложность, длительность, сезон)
- `route_tags` - связь маршрутов с тегами
- `audio_cache` - кеш сгенерированных аудиофайлов
- `user_audio_settings` - настройки аудио для пользователей
- `system_settings` - системные настройки бота (проверка подписки, ручная модерация, уведомления)
- `promo_codes` - промокоды с типами скидок (процентные, фиксированные, бесплатные)
- `promo_code_uses` - история использования промокодов пользователями
- `reviews` - отзывы пользователей о маршрутах

## 🔗 Как работает интеграция

### 1. Общая БД MySQL

Бот и сайт работают с **одной** базой `quest_bot`:

```python
# bot/bot/config.py
return f"mysql+aiomysql://user:password@localhost:3306/quest_bot"
```

```php
// Site/includes/db.php
new PDO("mysql:host=localhost;dbname=quest_bot", "user", "password");
```

### 2. Общая папка для фото

```python
# Бот сохраняет в:
/var/www/html/photos/{telegram_id}/point_{id}_{timestamp}.jpg
```

```php
// Сайт читает из:
<img src="/photos/{telegram_id}/point_{id}_{timestamp}.jpg">
```

### 3. Авторизация через токены

**В боте:**
```
Пользователь → /web
Бот → генерирует токен в таблицу user_sessions
Бот → отправляет кнопку с ссылкой https://site.ru/auth/telegram?token=XXX
```

**На сайте:**
```php
// Site/auth/telegram.php
Проверяет токен → Авторизует пользователя → Перенаправляет на dashboard
```

## ⚙️ Конфигурация

### Для сайта (.env в корне)

```env
# MySQL - ОБЩАЯ БД
DB_HOST=localhost
DB_NAME=quest_bot
DB_USER=quest_bot_user
DB_PASS=your_password

# Telegram Bot
BOT_TOKEN=123456:ABC-DEF...
BOT_USERNAME=your_bot_username

# Site
SITE_URL=https://yourdomain.com
DEBUG=false
```

### Для бота (bot/.env)

```env
# Telegram Bot
BOT_TOKEN=123456:ABC-DEF...
ADMIN_IDS=123456789,987654321

# MySQL - ТА ЖЕ БД
DB_HOST=localhost
DB_PORT=3306
DB_USER=quest_bot_user
DB_PASSWORD=your_password
DB_NAME=quest_bot

# Paths - корень сайта
BASE_DIR=/var/www/html

# Web Integration
SITE_URL=https://yourdomain.com
API_SECRET=random_secret_key
```

## 🎯 Тестовые данные

После импорта schema.sql будут созданы:

**Город:** Москва
**Маршрут:** Исторический центр (399₽)
**Точки:** 3 (Красная площадь, Храм Василия Блаженного, ГУМ)
**Достижения:** 10 штук (от "Первые шаги" до "Легенда")

## 🔧 Управление
### Бот

**Полезные команды:**
```bash
screen -ls                    # Список сессий
screen -r questbot            # Подключиться
# Внутри screen: Ctrl+A, затем D - отключиться
```

### Очистка временных файлов

**Добавить в cron:**
```bash
crontab -e
```

```cron
# Очистка каждый день в 3:00
0 3 * * * /var/www/html/bot/venv/bin/python /var/www/html/bot/bot/utils/cleanup.py

# Бэкап БД каждый день в 4:00
0 4 * * * mysqldump -u quest_bot_user -pPASSWORD quest_bot > /var/www/html/backups/db_$(date +\%Y\%m\%d).sql
```

```bash
# Проверка бота
screen -r questbot
tail -f /var/www/html/bot/bot.log

# Проверка БД
mysql -u quest_bot_user -p quest_bot
> SELECT COUNT(*) FROM users;
> SELECT * FROM routes;

# Очистка временных файлов
cd /var/www/html/bot
source venv/bin/activate
python bot/utils/cleanup.py

# Перезапуск бота
screen -r questbot
# Ctrl+C для остановки
```

## 📱 Использование

### Пользователь:

1. **В Telegram:** отправить `/start` боту
2. Выбрать город и маршрут
3. Оплатить 399₽ (или использовать промокод `/promo`)
4. Пройти квест:
   - **Фото-задания** - сфотографироваться на точке с определенной позой
   - **Текстовые задания** - ответить на вопрос или загадку текстом
   - **Использовать подсказки** - кнопка "💡 Подсказка" доступна в меню каждой точки
   - Подсказки имеют 3 уровня: легкая, средняя, детальная (с мини-картами и фото)
   - Ограничение по количеству подсказок на маршрут (настраивается администратором)
5. Получить достижения и сертификат о прохождении маршрута

6. **На сайте:** `/web` в боте → войти → смотреть статистику

### Администратор:

1. **В боте:** `/admin` → управление городами/маршрутами/точками/пользователями/промокодами
   - Управление подсказками для точек
   - Управление тегами и фильтрами
   - **Управление промокодами** - создание, просмотр, редактирование, удаление
   - Блокировка пользователей (1ч, 1д, 1м, 1г, навсегда)
   - Генерация аудиогидов через TTS
   - **Настройки системы:**
     - Включение/выключение проверки подписки на канал
     - Включение/выключение ручной модерации фото (все фото сразу админам)
     - Включение/выключение уведомлений о перезапуске
2. **Веб-админ-панель:** `/admin` в боте → получить ссылку → войти на сайт
   - Дашборд с аналитикой и графиками
   - Массовое управление маршрутами и точками
   - **Управление городами** - создание, редактирование, удаление, статистика
   - Модерация фото с фильтрами
   - Управление тегами и категориями
   - Управление аудио для точек
   - **Управление промокодами** - создание с привязкой к маршрутам
   - Список пользователей с блокировкой/разблокировкой
3. **Проверка фото:**
   - **Автоматический режим** - при ошибках проверок админы получают уведомления с кнопками "Принять/Отклонить"
   - **Ручной режим** - все фото сразу идут админам без автопроверок (для презентаций/демо)

## 🏆 Достижения (автоматически)

- 🎯 **Первые шаги** - завершите 1 маршрут
- 🗺️ **Исследователь** - пройдите 5 маршрутов
- 🏆 **Мастер квестов** - 10 маршрутов
- 📍 **Коллекционер** - 50 точек
- 📸 **Фотограф** - 100 фото
- 💯 **Перфекционист** - 100% прохождение
- ⚡ **Быстрый** - быстрее ожидаемого времени
- 🌙 **Ночной странник** - квест ночью (22:00-06:00)
- 🌅 **Ранняя пташка** - квест до 8:00
- 👑 **Легенда** - все достижения

## 🔧 Ключевые особенности и реализованные фиксы

### Система подсказок

**3 уровня подсказок:**
- 💡 **Легкая** - общее направление, намек на локацию
- 🔦 **Средняя** - более конкретное указание, детали окружения
- 🎯 **Детальная** - почти точное местоположение, мини-карта с отметкой

**Особенности:**
- Подсказки доступны **прямо в меню прохождения квеста** - кнопка "💡 Подсказка" появляется автоматически для каждой точки
- Кнопка подсказки отображается только если для точки есть подсказки
- Показывает количество оставшихся подсказок на маршрут (например: "💡 Подсказка (2 осталось)")
- Ограничение по количеству подсказок на маршрут (настраивается администратором в поле `max_hints_per_route`)
- Каждая подсказка может быть использована только один раз
- Подсказки показываются последовательно (сначала легкая, потом средняя, потом детальная)
- Детальные подсказки могут содержать мини-карту с отметкой местоположения
- **Фото-подсказки** - возможность прикреплять изображения к любому уровню подсказки для визуальной помощи
- После просмотра подсказки можно вернуться к заданию кнопкой "◀️ Назад к заданию"

**Использование:**
1. Во время прохождения квеста нажмите кнопку "💡 Подсказка" в меню точки
2. Выберите уровень подсказки (если доступно несколько) или получите единственную доступную
3. Получите текст подсказки, фото (если есть) и мини-карту (если есть)
4. Нажмите "◀️ Назад к заданию" чтобы вернуться к заданию точки
5. Найдите локацию используя подсказку

- Привязка слов: каждое слово связано с точкой маршрута
- Генерация изображений: автоматическое создание всех прогрессивных изображений
- Настройки: финальное слово, бонусное сообщение

### Генерация сертификатов

**Возможности:**
- Автоматическое создание PDF сертификатов о прохождении маршрутов
- Поддержка русского и английского языков
- Отображение на веб-сайте в разделе "Сертификаты"
- Красивый дизайн с логотипом и информацией о маршруте
- Мобильная адаптация для просмотра на телефонах

### Обработка и улучшение фото

**Автоматические улучшения:**
- Коррекция яркости для лучшей видимости
- Улучшение контраста для четкости
- Повышение резкости для детализации
- Настройка насыщенности для более ярких цветов
- Применяется автоматически при сохранении фото пользователя

### Машинное зрение (проверка фото)

**Локация:**
- **CLIP** (openai/clip-vit-base-patch32) — сравнение фото пользователя с текстовым описанием объекта/точки; порог схожести настраивается.
- **Fallback:** при недоступности CLIP (память, ошибка загрузки) или при отсутствии описания — проверка по эталонным фото через **AKAZE/ORB** (OpenCV).

**Люди и позы:**
- **YOLOv8n ONNX** — детекция людей на фото, подсчёт количества (в т.ч. для заданий «сфотографироваться с N людьми»).
- **MoveNet ONNX** — проверка позы (соответствие заданной позе на точке).
- Модели легковесные, подходят для shared hosting; после проверки сессия YOLO выгружается для экономии памяти.

**Оптимизация под хостинг:**
- MoveNet и YOLO в формате ONNX (без тяжёлого PyTorch при инференсе поз/людей).
- Явное освобождение памяти после проверки (`gc.collect()`, выгрузка сессии YOLO, при наличии — `malloc_trim`).
- Ограничение потоков (OPENBLAS, OMP), уменьшение размера изображений перед обработкой.

### Проверка фото (2 режима работы)

**Режим 1: Автоматическая проверка с резервной модерацией (по умолчанию)**
1. Антифрод (повторное использование, EXIF, timing)
2. Количество людей (YOLOv8n ONNX)
3. Поза (MoveNet ONNX)
4. Локация (CLIP по описанию объекта или AKAZE/ORB по эталонам)
5. **Автоматическое улучшение фото** — коррекция яркости, контраста, резкости, насыщенности
6. При ошибках → фото отправляется админам с кнопками "Принять/Отклонить"

**Режим 2: Ручная модерация (для презентаций/демо)**
- Включается в `/admin` → Настройки → "Включить ручную модерацию фото"
- **Все фото сразу идут админам без автопроверок**
- Пользователь видит только "Проверяю фото..." без упоминания ошибок
- Админы видят: фото, маршрут, точку, информацию о пользователе
- Кнопки: "✅ Принять" / "❌ Отклонить"
- **Создаёт впечатление автоматической проверки** - идеально для демонстрации работы бота

**Ручная проверка админом (в обоих режимах):**
- Админы видят: фото, маршрут, точку, причину отклонения (если есть), количество людей, позу, совпадение локации
- Кнопки: "✅ Принять" / "❌ Отклонить"
- При принятии: фото сохраняется, точка засчитывается, пользователь переходит дальше
- При отклонении: пользователь получает уведомление с рекомендациями (в автоматическом режиме) или просто повторяет попытку (в ручном режиме)

**Проверка количества людей:**
- YOLOv8n ONNX для детекции людей; толерантность: принимается `count >= max(1, min_people - 1)` для групповых фото.

### Оптимизация для shared hosting

- **MoveNet ONNX** и **YOLOv8n ONNX** — лёгкий инференс без полного PyTorch-графа при проверке поз/людей.
- **CLIP** загружается при необходимости; при нехватке памяти используется fallback на эталонные фото (AKAZE/ORB).
- **Последовательная обработка** проверок, **явное освобождение памяти** после проверки (выгрузка сессии YOLO, `gc.collect()`, при наличии — `malloc_trim`).
- **Уменьшение размера изображений** перед обработкой.
- **Ограничение потоков** (`OPENBLAS_NUM_THREADS=1`, `OMP_NUM_THREADS=1`).

### Веб-сайт

- **Тема оформления:** применяется на всех страницах, мгновенное переключение без перезагрузки
- **Мобильное меню:** работает корректно на всех устройствах
- **Аватар:** круглый, без искажений, возможность загрузки собственного
- **SEO:** robots.txt, sitemap.xml, кастомные страницы ошибок
- **Загрузка аватара:** автоматическая обработка (300x300px), обрезка по центру

### Уведомления админам

- При запуске/перезапуске бота (можно отключить в настройках)
- При критических ошибках (с логом)
- При ошибках проверки фото (для ручной проверки) - в автоматическом режиме
- При любом фото (для модерации) - в ручном режиме

### Проверка подписки на канал

**Гибкая настройка:**
- Включается/выключается в `/admin` → Настройки → "Включить/выключить проверку подписки"
- По умолчанию использует значение из `.env` файла (`REQUIRE_SUBSCRIPTION=true/false`)

**Умная блокировка:**
- **Блокирует:** начало нового квеста (кнопка "Начать квест")
- **НЕ блокирует:** просмотр статистики, настройки, информацию о боте, выбор городов/маршрутов, команду `/top`
- Пользователь может изучить бота и выбрать маршрут **до** подписки на канал

**Для пользователя:**
- При попытке начать квест без подписки → показывается сообщение с кнопками
- "📢 Перейти в канал" - прямая ссылка на канал
- "✅ Я подписался" - проверка подписки
- После подписки - полный доступ ко всем функциям

## ❓ FAQ

### Как получить токен бота?

@BotFather → `/newbot` → скопировать токен

### Как узнать свой Telegram ID?

@LeadConverterToolkitBot → отправить сообщение /get_my_id → скопировать ID

### Почему не работает Login Widget?

1. Проверьте `BOT_USERNAME` в .env (без @)
2. Зарегистрируйте домен в BotFather (Bot Settings → Domain)
3. Убедитесь, что сайт работает по HTTPS

### Сайт показывает ошибку БД?

1. Проверьте настройки в `.env`
2. Убедитесь, что схема импортирована
3. Проверьте права пользователя БД

### Фотографии не отображаются?

```bash
# Установите права
chmod 755 /var/www/html/photos
chmod -R 755 /var/www/html/photos/
```

## 📝 Команды бота

**Пользовательские:**
- `/start` - начало работы
- `/web` - вход на сайт через Telegram Web App (открывается прямо в Telegram) или в браузере
- `/token` - банк токенов (пополнение, переводы, покупка экскурсий, история)
- `/promo` - ввести промокод для получения скидки
- `/top` - топ-10 популярных маршрутов
- `/review` - оставить отзыв о пройденном маршруте

**Админские:**
- `/admin` - админ-панель (управление городами, маршрутами, точками, пользователями, промокодами, подсказками, тегами, аудио, блокировками, настройками)
- `/web` - получить ссылку для входа в веб-админ-панель

| Проблема | Решение |
|----------|---------|
| Бот не запускается | Проверить .env, логи, установку зависимостей |
| Сайт не открывается | Проверить .env, права на файлы, PHP 7.4+ |
| Login не работает | Зарегистрировать домен в BotFather, HTTPS |
| БД ошибка | Проверить данные подключения, импорт схемы |
| Фото не видны | Права 755 на photos/, проверить пути в БД |
| Бот пропускает фото | Проверки исправлены, при ошибках отправляется админам |
| Админ не может принять фото | Исправлено: photo_file_id из сообщения |

## 🔐 Безопасность

- ✅ SQL-инъекции: PDO + SQLAlchemy ORM
- ✅ XSS: htmlspecialchars() везде
- ✅ Антифрод: хеши фото, timing, EXIF
- ✅ Токены: одноразовые, срок 5 минут
- ✅ Сессии: 90 дней, безопасное хранение
- ✅ API Secret: защита API endpoints
- ✅ .htaccess: защита конфигов
- ✅ Промокоды: одноразовое использование на пользователя, проверка срока действия
- ✅ Проверка подписки: гибкая настройка, защита от обхода
- ✅ Улучшение фото: автоматическая обработка для лучшего качества

## 💰 Монетизация

**Банк токенов:**
- Маршруты покупаются за **токены** (внутренняя валюта). Пополнение: **ЮKassa** (карта) или **Telegram Stars**.
- Команда `/token`: пополнение баланса, перевод токенов другому пользователю, покупка экскурсии, история операций.
- На сайте: вкладка «Банк» — баланс, история пополнений и покупок. В админке: раздел «Банк токенов» — балансы, транзакции, ручное начисление.

**Промокоды и скидки:**
- **Процентная скидка** - например, скидка 20% на любой маршрут
- **Фиксированная скидка** - например, скидка 100₽ на маршрут
- **Бесплатный маршрут** - промокод делает конкретный маршрут бесплатным
- Привязка к конкретному маршруту (опционально)
- Ограничение по количеству использований
- Ограничение по срокам действия (valid_from, valid_until)
- Одноразовое использование на пользователя

**Управление промокодами:**
- **В Telegram боте:** `/admin` → Промокоды → создать/просмотреть/редактировать/удалить
- **В веб-админке:** `/admin/promo_codes/` - создание с привязкой к маршрутам
- **Для пользователей:** `/promo` или кнопка "Ввести промокод" при выборе маршрута

**Настройка платежей:**
1. **ЮKassa:** зарегистрироваться на yookassa.ru, получить токен для Telegram Payments, добавить в bot/.env: `PAYMENT_PROVIDER_TOKEN=...`
2. **Telegram Stars:** включить в BotFather при необходимости; пополнение токенов через Stars обрабатывается в боте автоматически.